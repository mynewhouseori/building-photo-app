{
  "name": "building-photo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Documented building photo app for web, Android, and iOS.",
  "scripts": {
    "build:web": "node scripts/prepare-web.js",
    "sync": "npm run build:web && cap sync",
    "open:android": "npm run sync && cap open android",
    "open:ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
